package rocks.newsie.app.domain

import rocks.newsie.app.data.ApiClient

class FeedRepository(
    private val apiClient: ApiClient,
) {
    suspend fun doSomething() {
        // TODO: implement the repo API
    }
}